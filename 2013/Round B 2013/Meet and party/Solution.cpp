#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
    int T, B, x1, y1, x2, y2;
    cin >> T;
    for(int t = 1; t <= T; t++) {
        cin >> B >> x1 >> y1 >> x2 >> y2;
        int x, y ,d;
        cout << "Case #" << t << ": " << x << " " << y << " " << d << endl;
    }

    return 0;
}